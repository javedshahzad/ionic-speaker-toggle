import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capacitor.speakertoggle',
  appName: 'Speaker toggle',
  webDir: 'www'
};

export default config;
